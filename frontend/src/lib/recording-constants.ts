/** Snapshot interval for live preview capture during screen recording. */
export const FRAME_INTERVAL_SEC = 5;

/** Cap frames sent to Groq vision (5s interval → up to ~240s of coverage). */
export const MAX_FRAMES = 48;

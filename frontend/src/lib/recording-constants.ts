/** Snapshot interval for live preview capture during screen recording. */
export const FRAME_INTERVAL_SEC = 2;

/** Cap frames sent to Groq vision (2s interval → up to ~96s of coverage). */
export const MAX_FRAMES = 48;
